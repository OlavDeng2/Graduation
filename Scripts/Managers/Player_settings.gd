extends Node

#player reference so we know what player to change settings on
var player = null


# Called when the node enters the scene tree for the first time.
func _ready():
	pass
	#player = find_node("Player", true)
